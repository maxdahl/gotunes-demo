{
  "name": "gotunes",
  "version": "0.0.1",
  "description": "A music streaming app for all major streaming services",
  "main": "index.js",
  "scripts": {
    "server": "npm start --prefix backend",
    "client": "npm start --prefix frontend",
    "dev": "concurrently \"npm run server\" 'npm run client'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
